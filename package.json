{
  "name": "logseq-autoclass-plugin",
  "version": "v1.0.0",
  "author": "JWangL5",
  "description": "🥳 Automatically add the class property",
  "license": "MIT",
  "scripts": {
    "watch": "tsc -w",
    "build": "esbuild index.ts --bundle --outfile=dist/index.js --platform=node --format=cjs"
  },
  "devDependencies": {
    "@logseq/libs": "^0.0.17",
    "esbuild": "^0.24.2",
    "typescript": "^5.0.0"
  },
  "browserslist": [
    "chrome 80"
  ],
  "logseq": {
    "id": "logseq-autoclass-plugin",
    "main": "./index.html",
    "icon": "logo.png"
  }
}
